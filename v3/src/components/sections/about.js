import React, { useEffect, useRef } from 'react';
import { StaticImage } from 'gatsby-plugin-image';
import styled from 'styled-components';
import { srConfig } from '@config';
import sr from '@utils/sr';
import { usePrefersReducedMotion } from '@hooks';

const StyledAboutSection = styled.section`
  max-width: 900px;

  .inner {
    display: grid;
    grid-template-columns: 3fr 2fr;
    grid-gap: 50px;

    @media (max-width: 768px) {
      display: block;
    }
  }
`;
const StyledText = styled.div`
  ul.skills-list {
    display: grid;
    grid-template-columns: repeat(2, minmax(140px, 200px));
    grid-gap: 0 10px;
    padding: 0;
    margin: 20px 0 0 0;
    overflow: hidden;
    list-style: none;

    li {
      position: relative;
      margin-bottom: 10px;
      padding-left: 20px;
      font-family: var(--font-mono);
      font-size: var(--fz-xs);

      &:before {
        content: '▹';
        position: absolute;
        left: 0;
        color: var(--sydneyblue);
        font-size: var(--fz-sm);
        line-height: 12px;
      }
    }
  }
`;
const StyledPic = styled.div`
  position: relative;
  max-width: 300px;

  @media (max-width: 768px) {
    margin: 50px auto 0;
    width: 70%;
  }

  .wrapper {
    ${({ theme }) => theme.mixins.boxShadow};
    display: block;
    position: relative;
    width: 100%;
    border-radius: var(--border-radius);
    background-color: #CED4DA;

    &:hover,
    &:focus {
      outline: 0;
      transform: translate(-4px, -4px);

      &:after {
        transform: translate(8px, 8px);
      }

      .img {
        filter: none;
        mix-blend-mode: normal;
      }
    }

    .img {
      position: relative;
      border-radius: var(--border-radius);
      mix-blend-mode: multiply;
      filter: grayscale(100%) contrast(1);
      transition: var(--transition);
    }

    &:before,
    &:after {
      content: '';
      display: block;
      position: absolute;
      width: 100%;
      height: 100%;
      border-radius: var(--border-radius);
      transition: var(--transition);
    }

    &:before {
      top: 0;
      left: 0;
      background-color: var(--night);
      mix-blend-mode: screen;
    }

    &:after {
      border: 2px solid var(--sydneyblue);
      top: 14px;
      left: 14px;
      z-index: -1;
    }
  }
`;

const About = () => {
  const revealContainer = useRef(null);
  const prefersReducedMotion = usePrefersReducedMotion();

  useEffect(() => {
    if (prefersReducedMotion) {
      return;
    }

    sr.reveal(revealContainer.current, srConfig());
  }, []);

  const skills = ['Python', 'Tableau', 'Pandas', 'Scipy', 'Numpy', 'Scikit-Learn', 'NLTK', 'Flask', 'Jupyter Notebook', 'Tensorflow', 'Matplotlib', 'Plotly' ];
  const pskills = ['SQL','MongoDB','Azure','Power BI','Google Cloud','Kubernetes','Docker','dbt Cloud','PySpark','Apache Hadoop','Apache Airflow','PostgreSQL', 'Snowflake','Numbers','C++', 'CSS', 'MATLAB', 'MySQL', 'SharePoint', 'Streamlit' ,'HTML', 'React Js', 'Wireshark', 'Wordpress', 'RMS Management System', 'OpenOffice', 'MS Excel'];

  return (
    <StyledAboutSection id="about" ref={revealContainer}>
      <h2 className="numbered-heading">About Me</h2>

      <div className="inner">
        <StyledText>
          <div>
            <p>
              Hey there! I'm Hemang, a data scientist with a passion for turning numbers into narratives. Recently, I wrapped up a stint as a Data Analyst at <a href="#">UKO</a> in Sydney, where I helped shape smarter decisions for over 30 properties.
            </p>

            <p>
              I'm a proud alumnus of <a href="https://www.ucdavis.edu">UC Davis</a>, and <a href="https://www.uts.edu.au">University of Technology Sydney</a>, where I honed my skills and developed a love for data.
            </p>
            <p>
              I'm always on the lookout for new adventures and connections.
            </p>
            <p>Here are a few technologies I’ve been working with recently:</p>
          </div>

          <ul className="skills-list">
            {skills && skills.map((skill, i) => <li key={i}>{skill}</li>)}
          </ul>
          <div>
            <br></br>
            <p>Here are a few technologies I’ve worked with in past:</p>
          </div>
          <ul className="skills-list">
            {pskills && pskills.map((pskill, i) => <li key={i}>{pskill}</li>)}
          </ul>
        </StyledText>

        <StyledPic>
          <div className="wrapper">
            <StaticImage
              className="img"
              src="../../images/me.png"
              width={500}
              quality={95}
              formats={['AUTO', 'WEBP', 'AVIF']}
              alt="Headshot"
            />
          </div>
        </StyledPic>
      </div>
    </StyledAboutSection>
  );
};

export default About;
